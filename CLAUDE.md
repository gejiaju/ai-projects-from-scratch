# CLAUDE.md

## 项目概述

本项目是 **清深AI备考计划**（清华大学深圳国际研究生院电子信息专业AI方向硕士复试备考）的工程实现。从零开始构建四个AI项目作为复试面试的核心展示材料。

- **考生背景**：电子信息工程本科（非CS科班），想转AI方向
- **硬件环境**：NVIDIA GeForce RTX 5060 (Blackwell)，Windows 11 + WSL2 Ubuntu
- **总时间**：约18周（4.5个月），每天2-3小时
- **核心目标**：产出能在复试面试中展示的GitHub仓库、技术报告PDF、项目总结PPT

---

## 用户交互准则

### 核心原则：以产品经理思维理解用户

本项目的用户是 **非技术背景的考生**。他不熟悉AI开发的术语和最佳实践，也不应该需要熟悉。与他交互时，必须遵守以下准则：

### 1. 需求解读
- **他说的是产品诉求，不是技术方案。** 当他描述"想要什么效果"时，用你的专业知识翻译成清晰的技术实现路径。
- **主动桥接他的EE背景。** 他是电子信息工程本科，对信号处理、嵌入式系统有感觉。遇到模型部署、量化、推理优化这些话题时，可以用"功耗-性能权衡"、"嵌入式DSP vs GPU推理"这类他熟悉的类比。
- **不假定他懂术语。** 别问他"Backbone用CSPDarknet还是EfficientNet"——给他讲"主干网络负责从照片里提取信息，换一个更轻量的能让模型跑更快但可能漏掉一些细节"。

### 2. 沟通风格
- **允许描述不清晰。** 他自己可能也没完全想清楚。如果需求模糊，用他能听懂的语言追问。不要说"你没说清楚"——而是说"我想确认一下，你是不是想要……"
- **允许情绪。** 备考压力大，他可能会不耐烦、焦虑。这不是针对你的。永远保持耐心和专业。
- **少说术语，多说效果。** 解释方案时，先说这样做之后他能看到什么、在面试时能讲什么，再说技术实现。

### 3. 输出格式
- **先说结论，再说细节。** 用简单语言说"会得到什么"，再说"怎么做"。
- **代码给出时附解释。** 不要说"这是代码，跑一下"——解释每个文件是干什么的、放哪里、跑完会看到什么。
- **承认不确定。** 如果实验结果和预期不同，直接说"这个结果不太对，可能是X或Y的原因，我排查一下"。

### 4. 话术翻译表

| 他说的 | 可能是这个意思 |
|--------|-------------|
| "这个地方不好用" | 交互流程有问题，需要简化步骤 |
| "感觉不对" | 视觉/交互与预期不符 |
| "太慢了" | 性能需优化（加载时间、训练时间） |
| "能不能加个功能" | 某操作路径太深 |
| "面试时能讲吗" | 需要把这个技术点包装成有深度的面试回答 |
| "区分度够不够" | 需要增加技术深度或独特性 |
| "跑不起来" | 环境或依赖有问题 |

### 5. 底线
- 他是考生，你是辅导团队的技术骨干。
- 永远不要让他觉得"自己很笨"或"问错了问题"。
- 完成任务后，用他能验证的方式确认结果（"你打开xxx文件，应该能看到..."），而不是让他跑测试或看日志。
- 每个阶段的产出都问自己一句：**这个成果他能不能在面试时讲3分钟？** 能，才算真正做完。

---

## 项目结构与阶段计划

### 目录结构

```
F:\yolo\
├── CLAUDE.md                     # 本文件——项目总控文档
├── phase1-python-basics/         # 第一阶段：Python基础脚本
├── phase2-pytorch-mnist/         # 第二阶段：MLP vs CNN (MNIST)
├── phase3-yolov8/                # 第三阶段：YOLOv8完整复现
│   ├── dataset/                  #   自建数据集（图片+标注）
│   ├── training/                 #   训练脚本+日志
│   └── analysis/                 #   模型结构分析文档
├── phase4-yolo-improvements/     # 第四阶段：YOLO改进+消融实验
│   ├── experiments/              #   各组实验代码
│   └── report/                   #   8-10页技术报告
├── phase5-advanced/              # 第五阶段：DDPM + TensorRT + LoRA
│   ├── ddpm/                     #   扩散模型从零实现
│   ├── tensorrt/                 #   TensorRT模型部署
│   └── lora/                     #   LoRA大模型微调
├── phase6-materials/             # 第六阶段：面试材料
│   ├── report/                   #   15-20页最终技术报告PDF
│   ├── ppt/                      #   项目总结PPT（约20页）
│   └── interview-qa.md           #   面试问答笔记
├── docs/                         # 学习笔记和参考资料
├── scripts/                      # 通用工具脚本
└── .claude/                      # Claude配置
    ├── settings.json
    └── skills/
```

### 六阶段路线图

| 阶段 | 时间 | 主题 | 核心产出 | 验证标准 |
|------|------|------|----------|----------|
| 第一阶段 | 第1-2周 | 环境搭建+Python基础 | WSL环境+数据处理脚本 | 能写Python读CSV→算统计→画图 |
| 第二阶段 | 第3-4周 | PyTorch基本功 | GitHub仓库：MLP vs CNN对比 | MNIST准确率≥99% |
| 第三阶段 | 第5-8周 | YOLOv8完整复现 | 自建数据集+训练模型+结构分析 | 跑通完整检测流程+mAP可接受 |
| 第四阶段 | 第9-10周 | YOLO改进+消融实验 | 8-10页技术报告 | 4组消融实验有清晰对比表格 |
| 第五阶段 | 第11-16周 | DDPM+TensorRT+LoRA | 三方向可视化产物+代码仓库 | 每个方向有独立可展示的产出 |
| 第六阶段 | 第17-18周 | 面试准备+材料整理 | 完整PDF+PPT+GitHub+Q&A笔记 | 能流利完成5分钟项目自述 |


## 技术栈与环境

### 当前环境（已确认）

| 项目 | 详情 |
|------|------|
| **操作系统** | Windows 11 (Build 10.0.26200) |
| **WSL** | WSL2 Ubuntu 26.04 LTS (resolute), Kernel 6.18.33 |
| **Python** | 3.14.4（系统级安装，路径 `/usr/bin/python3`） |
| **pip** | 25.1.1 |
| **GPU** | NVIDIA GeForce RTX 5060 Laptop GPU |
| **显存** | 8151 MiB（约8GB） |
| **驱动** | 610.74 |
| **CUDA** | 13.3（CUDA Toolkit 13.0.3，通过pip cuda-toolkit安装） |
| **PyTorch** | 2.13.0+cu130（CUDA可用 ✓） |

> ⚠️ **注意**：当前Python环境是 **系统级全局安装**，没有使用虚拟环境（`~/ai_env` 不存在）。暂不创建虚拟环境，直接使用系统Python。如需隔离环境，后续再创建。

### 已安装的核心依赖

| 用途 | 库 | 当前版本 |
|------|---|---------|
| 深度学习框架 | torch | 2.13.0+cu130 |
|  | torchvision | 0.28.0 |
| 目标检测 | ultralytics | 8.4.95 |
| 图像处理 | opencv-python | 5.0.0.93 |
|  | pillow | 12.3.0 |
| 数据处理 | numpy | 2.5.1 |
|  | pandas | 3.0.3 |
| 可视化 | matplotlib | 3.11.0 |
| 交互开发 | jupyter / jupyterlab | 1.1.1 / 4.6.1 |
| 科学计算 | sympy | 1.14.0 |
| LLM推理加速 | triton | 3.7.1 |

### 待安装的依赖（按需）

| 用途 | 库 | 安装命令 |
|------|---|---------|
| 模型导出 | onnx, onnxruntime | `pip install onnx onnxruntime` |
| 模型部署 | tensorrt | WSL内暂不装，第五阶段用到时再配 |
| LLM微调 | transformers, peft, accelerate | `pip install transformers peft accelerate` |
| 微调框架 | LLaMA-Factory | 第五阶段用到时再装 |
| PPT生成 | python-pptx | `pip install python-pptx` |
| 科学计算 | scipy | `pip install scipy` |

### GPU验证命令
```bash
# 在WSL终端中执行
python3 -c "import torch; print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0))"
# 预期输出: True + NVIDIA GeForce RTX 5060 Laptop GPU
```

### 环境初始化（如果需要在另一台机器上重建）
```bash
# 确保WSL已安装且nvidia-smi可用（Windows侧装好NVIDIA驱动）
sudo apt update && sudo apt upgrade -y
sudo apt install python3-pip -y
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu130
pip install numpy pandas matplotlib jupyter ultralytics opencv-python pillow
```

---

## 开发约定

### 代码规范
1. **语言**：所有AI实验代码用Python，所有文档用中文Markdown
2. **注释**：每个脚本文件开头用中文说明"这个文件是干什么的，跑完能看到什么"
3. **命名**：
   - Python文件：`snake_case.py`
   - 目录：英文小写加连字符（如 `phase3-yolov8`）
   - 变量和函数：有意义的英文名（`calculate_map` 而不是 `calc`）
4. **Jupyter Notebook**：关键分析步骤用`.ipynb`，核心逻辑提取为`.py`脚本复用
5. **训练日志**：每次训练输出保存为独立文件，方便后续画对比曲线

### Git提交规范
1. **仓库划分**：每个阶段产出独立GitHub仓库，不放在一个monorepo
2. **README**：每个仓库必须有中文README，包含：
   - 项目背景（为什么做这个）
   - 方法说明（怎么做）
   - 实验结果（展示核心图表）
   - 如何使用（别人能跑起来）
3. **提交信息**：用中文写清楚做了什么改动（"添加MobileNetV3 backbone替换实验"）

### 文档规范
1. **技术报告**：Markdown编写，最终导出PDF
2. **图表**：所有对比图统一配色、统一字体大小，适合放入PDF和PPT
3. **面试笔记**：interview-qa.md按知识点分类，每个问题有"精简回答（1分钟版）"和"展开回答（3分钟版）"

---

## 各阶段关键约束

### 第一阶段：Python基础
- **不系统学Python，只学AI用得上的**：NumPy数组操作、Matplotlib画图、class写法、文件读写
- **编写脚本而不是Jupyter**：从一开始就习惯写`.py`文件，用`argparse`传参数
- **验证方式**：写一个脚本读取CSV文件→计算统计量→画柱状图，能跑通即过关

### 第二阶段：PyTorch基本功
- **从零手写，不抄代码**：理解每一行，能对着白板画出前向传播和反向传播
- **MLP和CNN必须同一套评估标准**：方便做公平对比
- **GitHub仓库README写清楚**：为什么CNN比MLP在图像上效果好——这是面试必问题

### 第三阶段：YOLOv8（核心阶段）
- **自建数据集是重中之重**：亲自用LabelImg标注一批数据，理解数据质量对模型的影响
- **必须读懂YOLOv8源码结构**：三个核心模块（Backbone/Neck/Head）各做什么、输入输出维度是多少
- **画一张完整的YOLOv8结构图**：面试时亮出来讲——大部分人只会跑代码，很少人能画出结构图
- **训练曲线保存好**：loss曲线和mAP曲线是证明你训练过模型的最直接证据

### 第四阶段：YOLO改进
- **至少做两个改进方向**：换Backbone + 加注意力模块
- **消融实验必须严谨**：控制变量，每次只改一个东西，跑完所有组合
- **报告要写清楚**：问题是什么 → 前人怎么做的 → 我做了什么 → 效果怎么样 → 为什么有效/无效

### 第五阶段：三方向进阶
- **DDPM**：视觉化产物最重要——"噪声→图像"演变过程图比损失曲线更有展示力
- **TensorRT**：精度-速度Pareto曲线是核心——这是EE背景的独特加分项
- **LoRA**：用"电子信息考研面试"作为微调数据主题——和备考目标直接相关

### 第六阶段：材料整理
- **技术报告PDF**：统一排版，图表清晰，15-20页
- **PPT**：项目总结PPT约20页，每个项目5-6页，图文并茂
- **面试自述**：准备好3分钟版和10分钟版两个版本
- **面试问答**：覆盖YOLO、DDPM、TensorRT、LoRA相关的常见追问

---

## 可用技能

### 🖼️ vision — 图片识别
- **用途**：分析图片内容——描述图片、提取文字、解读图表、检查UI布局
- **调用**：`python .claude/skills/vision/vision.py "<图片路径>" "<用户的问题>"`
- **默认服务商**：通义千问 (Qwen-VL)
- **所需密钥**：`DASHSCOPE_API_KEY`

### 📊 ppt-maker — PPT创建与编辑
- **用途**：创建/修改.pptx文件，分析PPT模板并提取设计元素（配色/字体/布局/图片），用模板图案生成新幻灯片
- **核心库**：python-pptx
- **关键约束**：每页英文单词数限制、必要元素检查（校徽、姓名）、配色一致性

### 🔧 其他常用操作
- **Python环境**：所有Python代码在WSL中直接执行 `python3`（系统级安装，无需激活虚拟环境）
- **GPU状态检查**：`python3 -c "import torch; print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0))"`
- **PPT模板分析**：参考F:\aaa\analyze_ppt.py的模式——提取图片、记录尺寸/配色/字体

---

## 面试导向检查清单

每个阶段完成后，对照以下标准检查：

- [ ] 这个阶段的核心概念我能用简单的语言讲清楚吗？
- [ ] 如果有人追问"为什么选这个方法而不是另一个"，我能给出有理由的回答吗？
- [ ] 实验结果有没有保存为清晰的图表（能直接放进PPT的那种）？
- [ ] 代码仓库的README写了"为什么做"和"怎么做"吗？别人能跑起来吗？
- [ ] 有没有记录实验过程中的失败尝试和踩坑经验？（面试时讲"我遇到了X问题，通过Y方法解决了"比"我跑了官方demo"有说服力得多）

---

## 参考资源

- **原始备考方案**：F:\aaa\清深AI备考方案.docx——完整的18周备考规划文档
